package client.model.enums;

/**
 * different types of direction for agent's next movement
 * default value for direction is CENTER (in case of null objects or maybe other problems)
 */
public enum Direction {
    CENTER,
    RIGHT,
    UP,
    LEFT,
    DOWN
}
